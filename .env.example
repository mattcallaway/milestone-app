# Safe-by-default configuration
# Copy this file to .env and modify as needed
# NEVER commit .env to version control

# Application Mode
# Set to "true" to enable write operations (destructive actions)
# Default: false (read-only mode for safe scanning)
WRITE_MODE=false

# API Configuration
API_HOST=127.0.0.1
API_PORT=8000

# Desktop App Configuration
ELECTRON_DEV_TOOLS=false

# Logging
LOG_LEVEL=info
